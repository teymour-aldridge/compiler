[package]
name = "fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
logic = { path = "../../logic" }

[dev-dependencies]
generator = { path = "../generator" }
fuzzcheck = { version = "0.13.0" }

[features]
