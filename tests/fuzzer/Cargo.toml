[package]
name = "fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
logic = { path = "../../logic" }

[dev-dependencies]
generator = { path = "../generator" }
fuzzcheck = { git = "https://github.com/loiclec/fuzzcheck-rs", rev = "2acc737" }

[features]
