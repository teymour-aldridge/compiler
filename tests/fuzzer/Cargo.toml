[package]
name = "fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
logic = { path = "../../logic" }

[dev-dependencies]
generator = { path = "../generator" }
fuzzcheck = { git = "https://github.com/loiclec/fuzzcheck-rs", rev = "54399b0" }

[features]
