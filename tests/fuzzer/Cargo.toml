[package]
name = "fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
logic = { path = "../../logic" }
generator = { path = "../generator" }
fuzzcheck = { version = "0.12.1", optional = true }
